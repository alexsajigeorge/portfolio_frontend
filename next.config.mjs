/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['assets.aceternity.com', 'pbs.twimg.com', 'aceternity.com' , 'images.unsplash.com'],
      },
};

export default nextConfig;